import NoteList from "@/components/NoteList";
import React from "react";

const Home = () => {
  return (
    <div className="h-screen">
      <NoteList />
    </div>
  );
};

export default Home;
